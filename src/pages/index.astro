---
import Layout from '../layouts/Layout.astro';
import InstrumentCard from '../components/InstrumentCard.astro';
import GalleryFilmstrip from '../components/GalleryFilmstrip.astro';
import Lightbox from '../components/Lightbox.astro';
import WorkshopModal from '../components/WorkshopModal.astro';
import { sanityClient } from '../lib/sanity';

// Static assets — 
import heroImage from '../assets/home-hero.jpg';
import heroImage2 from '../assets/home-our-story.jpg';
import workshopPoster from '../assets/luthier-workshop.png';
import logoIcon from '../assets/logo-icon.png';
import { Image } from 'astro:assets';

const navItems = [
  { label: 'Home', href: '/' },
  { label: 'Instruments', href: '/instruments' },
  { label: 'Repair & Maintenance', href: '/repair-maintenance' },
  { label: 'Workshops & Events', href: '/workshops-events' },
  { label: 'Our Story', href: '/our-story' },
  { label: 'Contact', href: '/contact' },
];

const services = [
  {
    title: 'Artesanal Sound',
    tag: 'Crafted with care',
    body: 'Explore the selection of handcrafted folk instruments at Taidesoitinpaja. Each item is carefully made in our luthier workshop, reflecting outstanding artistry and quality. Take a look at our stunning range of igils, bowed lyres, air tunable drums and other folk instruments for sale. Every piece combines tradition and innovation, expertly crafted with dedication and skill.',
  },
  {
    title: 'Customized Design',
    tag: 'Personal touch',
    body: 'Transform your instrument with a personal touch by customizing it to suit your preferences. At Taidesoitinpaja, we partner with you to create a distinctive piece that showcases your style and sound. Let your creativity shine through our customization service, allowing your instrument to reflect your unique identity and resonate with your spirit.',
  },
  {
    title: 'Repair Service',
    tag: 'Restore & revive',
    body: 'Trust Taidesoitinpaja for instrument repairs. we are specialized in restoring and reviving your instruments to their former glory. Let us breathe new life into your beloved instrument.',
  },
  {
    title: 'Workshops & Events',
    tag: 'Join our interactive courses',
    body: 'Immerse yourself in the art of crafting instruments by participating in our engaging workshops. Learn the intricate process of drum making, build yourself a jouhikko (bowed lyre) or learn throat singing. Experience the joy of creating your own musical masterpiece.',
  },
];

const featuredInstruments = await sanityClient.fetch(`
  *[_type == "instrument"] | order(order asc) [0...6] {
    name,
    "slug": slug.current,
    price,
    "categoryTitle": instrumentType->title,
    "image": images[0]
  }
`);

const homePage = await sanityClient.fetch(`
  *[_type == "homePage"][0] {
    previousWorks[] { image, caption },
    workInProgress[] { image, caption }
  }
`);

const upcomingWorkshops = await sanityClient.fetch(`
  *[_type == "workshopEvent" && defined(startDateTime) && startDateTime > now()]
    | order(startDateTime asc) [0...4] {
    title, shortDescription, startDateTime, location
  }
`);
---

<Layout title="Home">
  <header class="site-header">
    <div class="container site-header__inner">
      <a href="/" class="site-header__brand">
        <Image src={logoIcon} alt="Taidesoitinpaja" width={170} class="site-header__logo" />
      </a>

      <nav class="site-header__nav" id="site-nav">
        {navItems.map((item) => (
          <a href={item.href}>{item.label}</a>
        ))}
      </nav>
    </div>
  </header>

  <script>
    const navToggle = document.getElementById('site-nav-toggle');
    const nav = document.getElementById('site-nav');

    navToggle?.addEventListener('click', () => {
      const isOpen = nav?.classList.toggle('is-open');
      navToggle.setAttribute('aria-expanded', String(Boolean(isOpen)));
    });

    nav?.querySelectorAll('a').forEach((link) => {
      link.addEventListener('click', () => {
        nav.classList.remove('is-open');
        navToggle?.setAttribute('aria-expanded', 'false');
      });
    });
  </script>

  <section class="hero container">
    <div class="hero__text">
      <h1>Unveiling<br/>musical<br/>magic</h1>
      <p class="hero__sub">Handcrafted Unique Instruments
</p>
      <a href="/instruments" class="btn btn--solid">Learn More</a>
    </div>
    <div class="hero__image-wrap">
      <Image src={heroImage} alt="J-P Lilja's luthier workshop" class="hero__image" />
    </div>
  </section>

  <section class="instruments-band reveal">
    <div class="container">
      <div class="instruments-band__header">
        <div>
          <p class="eyebrow">Featured</p>
          <h2>Folk Instruments for Sale</h2>
        </div>
        <a href="/instruments" class="instruments-band__cta">View All Instruments →</a>
      </div>
      <div class="instruments-band__grid">
        {featuredInstruments.map((inst: any) => (
          <InstrumentCard
            name={inst.name}
            slug={inst.slug}
            price={inst.price}
            categoryTitle={inst.categoryTitle}
            image={inst.image}
          />
        ))}
      </div>
    </div>
  </section>

  <section class="services container reveal">
    <h2 class="services__heading">Luthier Workshop Services</h2>
    <div class="services__layout">
      <div class="services__image-wrap">
        <video
          class="services__image"
          src="/video/luthier-workshop-optimized.mp4"
          poster={workshopPoster.src}
          autoplay
          muted
          loop
          playsinline
          preload="metadata"
          aria-label="Inside J-P Lilja's luthier workshop"
        ></video>
      </div>
      <div class="services__list">
        {services.map((service) => (
          <div class="services__item">
            <h3>{service.title}</h3>
            <div class="services__tag">{service.tag}</div>
            <p>{service.body}</p>
            {service.title === 'Workshops & Events' && (
              <WorkshopModal events={upcomingWorkshops} />
            )}
          </div>
        ))}
      </div>
    </div>
  </section>

  {homePage?.previousWorks?.length > 0 && (
    <GalleryFilmstrip
      title="Previous works"
      items={homePage.previousWorks}
      imgWidth={500}
      imgHeight={650}
      itemWidth="260px"
    />
  )}

  {homePage?.workInProgress?.length > 0 && (
    <GalleryFilmstrip
      title="Work in Progress"
      items={homePage.workInProgress}
      imgWidth={360}
      imgHeight={450}
      itemWidth="200px"
      muted
    />
  )}

  <section class="our-story container reveal">
    <div class="our-story__content">
      <h2>Our Story</h2>
      <p class="eyebrow our-story__eyebrow">Crafting Musical Legacies</p>
      <p class="our-story__body">Taidesoitinpaja's journey is rooted in a deep passion 
        for preserving the rich essence of folk music through exquisitely handcrafted 
        instruments. Each creation embodies a unique story of heritage, quality, and 
        unwavering dedication to the art of instrument making. We invite you to embark 
        on this melodic voyage with us, where exceptional craftsmanship intertwines with boundless creativity, paving the way for a harmonious musical future.</p>
    </div>
    <div class="our-story__image-wrap">
      <Image src={heroImage2} alt="J-P Lilja at work in the workshop" class="our-story__image" />
    </div>
  </section>

  <!-- <div class="wide-banner">
    <Image src={heroImage2} alt="J-P Lilja at work in the workshop" class="wide-banner__image" />
  </div> -->

  <footer class="site-footer">
    <div class="container site-footer__inner">
      <div>
        <div class="site-footer__brand">Taidesoitinpaja</div>
        <p>Handcrafted folk instruments from a one-person luthier workshop in Finland.</p>
      </div>
      <div>
        <div class="site-footer__heading">Explore</div>
        <div class="site-footer__links">
          {navItems.map((item) => (
            <a href={item.href}>{item.label}</a>
          ))}
        </div>
      </div>
      <div>
        <div class="site-footer__heading">Contact</div>
        <p class="site-footer__contact">
          Taidesoitinpaja<br />
          J-P Lilja<br />
          Lahdenperä 142, 09120 Karjalohja, Finland<br /><br />
          +358 40 575 5403<br />
          taidesoitinpaja@gmail.com
        </p>
      </div>
    </div>
    <div class="container site-footer__bottom">
      © {new Date().getFullYear()} Taidesoitinpaja. All rights reserved.
    </div>
  </footer>
  <Lightbox />
</Layout>

<style>
  /* Header */
  .site-header {
    position: sticky;
    top: 0;
    z-index: 50;
    background:
      linear-gradient(rgb(36 27 20 / 12%), rgb(36 27 20 / 12%)),
      url('/images/wood-mahogany.webp') center / cover;
    backdrop-filter: blur(6px);
    border-bottom: 1px solid var(--color-border);
  }
  .site-header__inner {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-2);
    padding-block: var(--space-2);
  }
  .site-header__brand {
    display: flex;
    align-items: center;
    gap: var(--space-1);
    text-decoration: none;
    flex-shrink: 0;
  }
  .site-header__logo {
     background:
      linear-gradient(rgba(31, 36, 20, 0.06), rgb(36 27 20 / 6%)),
      url('/images/natural-wooden-background.webp') center / cover;
    padding-inline: 6px;
    height: auto;
    display: block;
    border: 1px solid #6b4c2e;
    border-radius: 5px;
    box-shadow:
      inset 0 1px rgb(255 245 220 / 32%),
      inset 0 -2px rgb(55 33 18 / 35%),
      0 3px 0 #4a2f1c,
      0 5px 10px rgb(0 0 0 / 25%);
  }
  .site-header__brand-text {
    display: flex;
    flex-direction: column;
    line-height: 1.1;
    text-shadow: 0 1px 1px rgb(0 0 0 / 35%);
  }
  .site-header__name {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 1.15rem;
    color: var(--color-cream);
  }
  .site-header__tagline {
    font-family: var(--font-mono);
    font-size: 0.6rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-text-muted);
  }
  .site-header__nav {
    background:
      linear-gradient(rgb(36 27 20 / 12%), rgb(36 27 20 / 12%)),
      url('/images/wood-mahogany.webp') center / cover;
    display: flex;
    align-items: center;
    gap: var(--space-3);
    min-width: 0;
  }
  .site-header__nav a {
    background:
      linear-gradient(rgb(36 27 20 / 12%), rgb(36 27 20 / 12%)),
      url('/images/wood-spruce.webp') center / cover;
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--color-cream);
    text-decoration: none;
    padding-block: 6px;
    padding-inline: 12px;
    border: 1px solid #6b4c2e;
    border-radius: 5px;
    box-shadow:
      inset 0 1px rgb(255 245 220 / 32%),
      inset 0 -2px rgb(55 33 18 / 35%),
      0 3px 0 #4a2f1c,
      0 5px 10px rgb(0 0 0 / 25%);
    text-shadow: 0 1px 1px rgb(0 0 0 / 35%);
    white-space: nowrap;
    transform: rotate(var(--nav-link-rotation, -0.25deg));
    transition: transform 150ms ease, box-shadow 150ms ease, border-color 150ms ease;
  }
  .site-header__nav a:nth-child(3n + 1) {
    --nav-link-rotation: -0.4deg;
  }
  .site-header__nav a:nth-child(3n + 2) {
    --nav-link-rotation: 0.3deg;
  }
  .site-header__nav a:nth-child(3n) {
    --nav-link-rotation: -0.2deg;
  }
  .site-header__nav a:hover {
    border-color: var(--color-brass);
    transform: translateY(-1px) rotate(var(--nav-link-rotation, -0.35deg));
    box-shadow:
      inset 0 1px rgb(255 245 220 / 38%),
      inset 0 -2px rgb(55 33 18 / 35%),
      0 4px 0 #4a2f1c,
      0 7px 12px rgb(0 0 0 / 28%);
  }
  .site-header__nav a:active {
    transform: translateY(2px) rotate(var(--nav-link-rotation, -0.35deg));
    box-shadow:
      inset 0 2px 4px rgb(55 33 18 / 45%),
      0 1px 0 #4a2f1c;
  }
  .site-header__icons {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    flex-shrink: 0;
  }
  .site-header__menu-toggle {
    display: none;
    align-items: center;
    justify-content: center;
    background: none;
    border: none;
    padding: 0;
    color: var(--color-cream);
    cursor: pointer;
  }
  .site-header__icon-link {
    display: flex;
    align-items: center;
    gap: 6px;
    color: var(--color-cream);
    text-decoration: none;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    white-space: nowrap;
  }

  @media (max-width: 1180px) and (min-width: 901px) {
    .site-header__nav {
      gap: var(--space-1);
      font-size: 0.8rem;
    }
  }
  @media (max-width: 900px) {
    .site-header__menu-toggle {
      display: flex;
    }
    .site-header__icon-label {
      display: none;
    }
    .site-header__nav {
      display: none;
      position: absolute;
      inset-inline: 0;
      top: 100%;
      flex-direction: column;
      align-items: flex-start;
      gap: 0;
      background: var(--color-bg);
      border-bottom: 1px solid var(--color-border);
      padding: var(--space-2) var(--space-3) var(--space-3);
    }
    .site-header__nav.is-open {
      display: flex;
    }
    .site-header__nav a {
      width: 100%;
      padding-block: 10px;
    }
  }

  /* Hero */
  .hero {
    display: grid;
    gap: var(--space-4);
    align-items: center;
    padding-block: var(--space-5) var(--space-5);
  }
  @media (min-width: 900px) {
    .hero {
      grid-template-columns: 0.85fr 1.15fr;
    }
  }
  .hero h1 {
    color: var(--color-brass);
    text-shadow:
    -1px -1px 1px oklch(10% 0.03 55.1 / 0.18),   /* shadow: darker, same hue */
     1px  1px 1px oklch(55% 0.04 60 / 0.7);   /* highlight: lighter, same hue */
  }
  .hero__sub {
    margin-block: var(--space-2) var(--space-3);
    font-size: 1.1rem;
  }

  .hero__image-wrap {
    border-radius: var(--radius);
    border: 12px solid #6b4c2e;
    box-shadow: 0 4px 12px rgb(0 0 0 / 20%);
    transition: transform 180ms ease, box-shadow 180ms ease;
    overflow: hidden;
    aspect-ratio: 16 / 16;
  }
  .hero__image-wrap :global(.hero__image) {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  /* Instruments band */
  .instruments-band {
    background:
      linear-gradient(rgb(36 27 20 / 12%), rgb(36 27 20 / 62%)),
      url('/images/striped-wood-texture.webp') center / cover;
    padding-block: var(--space-5);
  }
  .instruments-band__header {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: var(--space-2);
    flex-wrap: wrap;
    margin-bottom: var(--space-3);
  }
  .instruments-band__sub {
    margin-top: 0.4em;
  }
  .instruments-band__cta {
    font-family: var(--font-mono);
    font-weight: 700;
    font-size: 0.75rem;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: var(--color-cream);
    text-decoration: none;
    border-bottom: 2px solid var(--color-brass);
    padding-bottom: 3px;
    white-space: nowrap;
  }
  .instruments-band__grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-3);
  }
  @media (min-width: 700px) {
    .instruments-band__grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  /* Services */
  .services {
    padding-block: var(--space-5);
  }
  .services__heading {
    text-align: center;
    margin-bottom: var(--space-4);
  }
  .services__layout {
    display: grid;
    gap: var(--space-4);
  }
  .services__image-wrap {
    width: min(100%, 52rem);
    margin-inline: auto;
    border-radius: var(--radius);
    border: 12px solid #6b4c2e;
    box-shadow: 0 4px 12px rgb(0 0 0 / 20%);
    transition: transform 180ms ease, box-shadow 180ms ease;
    overflow: hidden;
    aspect-ratio: 4 / 3;
    margin-bottom: 32px;
  }
  .services__image-wrap :global(.services__image) {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }
  .services__list {
    display: grid;
    gap: var(--space-4);
  }
  @media (min-width: 800px) {
    .services__list {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }
  .services__item {
    border-bottom: 1px solid var(--color-border);
    padding-bottom: var(--space-3);
  }
  .services__item h3 {
    color: var(--color-brass);
    font-size: 1.375rem;
    margin-bottom: 4px;
  }
  .services__tag {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--color-text-muted);
    margin-bottom: var(--space-1);
  }
  .services__item p {
    margin-bottom: 0.9em;
  }

  /* Our Story */
  .our-story {
    display: grid;
    gap: var(--space-4);
    padding-block: var(--space-5);
  }
  .our-story__content {
    max-width: 38rem;
    text-align: left;
  }
  .our-story__eyebrow {
    margin-block: 0.4em var(--space-2);
  }
  .our-story__body {
    font-size: 1.0625rem;
    line-height: 1.75;
  }
  .our-story__image-wrap {
    overflow: hidden;
    border: 12px solid #6b4c2e;
    border-radius: var(--radius);
    box-shadow: 0 4px 12px rgb(0 0 0 / 20%);
  }
  .our-story__image-wrap :global(.our-story__image) {
    width: 100%;
    height: 100%;
    aspect-ratio: 5 / 3;
    object-fit: cover;
    display: block;
  }
  @media (min-width: 800px) {
    .our-story {
      grid-template-columns: minmax(0, 0.9fr) minmax(18rem, 1.1fr);
      align-items: start;
    }
    }

  /* Wide banner */
  .wide-banner {
    aspect-ratio: 21 / 6;
    overflow: hidden;
  }
  .wide-banner :global(.wide-banner__image) {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  /* Footer */
  .site-footer {
    background:
      linear-gradient(rgb(36 27 20 / 12%), rgb(36 27 20 / 12%)),
      url('/images/wood-mahogany.webp') center / cover;
    backdrop-filter: blur(6px);
    color: oklch(92% 0.015 80);
    padding: var(--space-5) 0 var(--space-2);
  }
  .site-footer__inner {
    display: grid;
    gap: var(--space-3);
  }
  @media (min-width: 700px) {
    .site-footer__inner {
      grid-template-columns: 1.3fr 1fr 1fr;
    }
  }
  .site-footer__brand {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 1.25rem;
    margin-bottom: 10px;
    color: oklch(92% 0.015 80);
  }
  .site-footer p {
    color: oklch(80% 0.015 80);
  }
  .site-footer__heading {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--color-brass);
    margin-bottom: var(--space-1);
  }
  .site-footer__links {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .site-footer__links a {
    font-size: 0.875rem;
    color: oklch(88% 0.015 80);
    text-decoration: none;
  }
  .site-footer__links a:hover {
    color: var(--color-brass);
  }
  .site-footer__contact {
    font-size: 0.875rem;
    line-height: 1.8;
    color: oklch(88% 0.015 80);
  }
  .site-footer__bottom {
    margin-top: var(--space-4);
    padding-top: var(--space-2);
    border-top: 1px solid oklch(40% 0.02 60);
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.03em;
    color: oklch(65% 0.015 75);
  }
</style>
